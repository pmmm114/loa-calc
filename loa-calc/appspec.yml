version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/loa-calc/ # 프로젝트 이름
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu

hooks:
  ValidateService:
    - location: scripts/ValidateService.sh
      timeout: 30
      runas: ubuntu
